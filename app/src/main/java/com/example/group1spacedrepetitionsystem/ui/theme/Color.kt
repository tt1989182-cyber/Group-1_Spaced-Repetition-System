package com.example.estudapp.ui.theme

import androidx.compose.ui.graphics.Color

// Bảng màu chính xác theo Logo StuWf4
val PrimaryBlue = Color(0xFF3498DB)   // Xanh dương sáng (phần trên logo)
val PrimaryGreen = Color(0xFF58D68D)  // Xanh lá tươi (phần dưới logo)
val DarkBlue = Color(0xFF2E86C1)      // Xanh dương đậm để làm điểm nhấn
val LightGreen = Color(0xFFABEBC6)    // Xanh lá nhạt cho nền

val Black = Color(0xFF2C3E50)         // Màu chữ tối hiện đại
val White = Color(0xFFFFFFFF)
val LightGray = Color(0xFFF4F7F6)     // Màu nền xám cực nhẹ
val ErrorRed = Color(0xFFE74C3C)      // Màu đỏ báo lỗi đồng bộ tông màu
